spring:
  config:
    activate:
      on-profile: prod
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: validate

security:
  cookie:
    refresh:
      secure: true
      same-site: None
